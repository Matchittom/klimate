
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; margin: 0; padding: 0; color: #333; background-color: #f4f4f4; }
        .container { max-width: 1200px; margin: auto; padding: 20px; }
        header, footer { background: #333; color: #fff; padding: 10px 0; text-align: center; }
        h1, h2 { color: #008000; }
        p { margin: 20px 0; }
        .button { display: inline-block; padding: 10px 20px; margin: 10px 0; background: #008000; color: white; text-decoration: none; border-radius: 5px; }
        img { width: 100px; height: 100px; border-radius: 50%; }
        ul { list-style-type: none; padding: 0; }
        li { margin: 10px 0; padding: 10px; background: white; border: 1px solid #ddd; }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>User Profile</h1>
        </div>
    </header>
    <div class="container">
        <div>
            <img src="{{user.profilePicture}}" alt="Profile Picture">
            <h2>{{user.username}}</h2>
            <p>Joined on {{user.dateJoined}}</p>
        </div>
        <h2>Favorites</h2>
        <ul>
            {{#each user.favorites}}
            <li>{{this.name}}</li>
            {{/each}}
        </ul>
        <h2>Reviews</h2>
        <ul>
            {{#each user.reviews}}
            <li>{{this.content}} - Rated: {{this.rating}}</li>
            {{/each}}
        </ul>
    </div>
    <footer>
        <div class="container">
            <p>Contact Us: info@klimate.com | Follow us on social media</p>
        </div>
    </footer>
</body>
</html>